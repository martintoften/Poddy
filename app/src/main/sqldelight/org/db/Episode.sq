CREATE TABLE episode (
    id TEXT NOT NULL UNIQUE PRIMARY KEY,
    podcast_id TEXT NOT NULL,
    title TEXT NOT NULL,
    description TEXT NOT NULL,
    pub_date INTEGER NOT NULL,
    duration INTEGER NOT NULL,
    image TEXT NOT NULL
);

selectAll:
SELECT * FROM episode;

selectById:
SELECT * FROM episode WHERE id = ?;

selectByIds:
SELECT * FROM episode WHERE id IN ?;

selectByPodcastId:
SELECT * FROM episode WHERE podcast_id = ? ORDER BY pub_date DESC;

insert:
INSERT OR REPLACE INTO episode(
    id, podcast_id, title, description, pub_date, duration, image
)VALUES?;

delete:
DELETE FROM episode WHERE id = ?;
